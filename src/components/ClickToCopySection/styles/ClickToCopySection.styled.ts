import styled from 'styled-components'

export const ClickToCopyStyled = styled.div`
  width: auto;
  height: auto;
  border-style: solid;

  height: 85px;
`

export const ClickToCopyTitle = styled.p`
  position: relative;
  left: 0;
  top: 0;
  margin: 0px;
  padding: 0px;
  height: auto;

  font-style: normal;
  font-weight: 500;
  font-size: 18px;
`

export const ClickToCopyLink = styled.a`
  position: relative;
  left: 0;
  bottom: 0;

  font-style: normal;
  font-weight: 300;
  font-size: 30px;

  cursor: copy;
`
